using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;

public class CreatePlayerOutlines : MonoBehaviour
{
    /*
    [SerializeField] PhotonView view;
    [SerializeField] Outline outline1;
    [SerializeField] Outline outline2;
    [SerializeField] Color myColor;
    [SerializeField] Color otherPlayerOutlineColor;
    [SerializeField, Range(0f, 5f)] float myLineWidth;
    [SerializeField, Range(0f, 5f)] float otherLineWidth;

    void Start()
    {
        //if is my outline
        if (view.IsMine)
        {
            
            //Outline Color
            outline1.OutlineColor = myColor;
            outline2.OutlineColor = myColor;

            //Outline Mode
            outline1.OutlineMode = Outline.Mode.SilhouetteOnly;
            outline2.OutlineMode = Outline.Mode.SilhouetteOnly;

            //Outline Width
            outline1.OutlineWidth = myLineWidth;
            outline2.OutlineWidth = myLineWidth;
            
        }
        else
        {
            
            //outline1.enabled = false;
            //outline2.enabled = false;
            //Outline Color
            outline1.OutlineColor = otherPlayerOutlineColor;
            outline2.OutlineColor = otherPlayerOutlineColor;

            //Outline Mode
            outline1.OutlineMode = Outline.Mode.OutlineVisible;
            outline2.OutlineMode = Outline.Mode.OutlineVisible;

            //Outline Width
            outline1.OutlineWidth = otherLineWidth;
            outline2.OutlineWidth = otherLineWidth;
            
            
        }
    }
    */
}
